from setuptools import setup, find_packages

setup(
    name='PropertyDataPuller',
    version='2.0.0',
    description='Pull data on properties from public property listing sites.',
    packages=find_packages(),
    include_package_data=True,
)
