from setuptools import setup, find_packages

setup(
    name='PolandPropertyDataPuller',
    version='1.0.0',
    description='Pull data on properties from public property listing sites.',
    packages=find_packages(),
    include_package_data=True,
)
